[servers]
  [servers.slack]
    [servers.slack.env]
      SLACK_BOT_TOKEN = "${MCPD__SLACK__SLACK_BOT_TOKEN}"
      SLACK_TEAM_ID = "${MCPD__SLACK__SLACK_TEAM_ID}"
  [servers.sqlite]
    args = ["--db-path=${MCPD__SQLITE__DB_PATH}"]
